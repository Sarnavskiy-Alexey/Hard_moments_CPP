#pragma once

#include <iostream>
#include <list>
#include "DZ2_matrix.h"

extern void AddAverage(std::list<double>& List);
extern std::ostream& operator<<(std::ostream& os, const std::list<double>& List);